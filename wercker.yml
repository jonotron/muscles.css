box: node
build:
  steps:
    - npm-install
    - hgen/gulp:
      tasks: docs
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GITHUB_TOKEN
        repo: jonotron/muscles.css
        basedir: gh-pages
